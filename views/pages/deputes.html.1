<html>
    <head>
        <link rel="stylesheet" href="{{=URL('static','css/page_deputes.css')}}">
    </head>
    <body>
        <select id="groupe-filter"><option value="ALL">Tous les groupes</option>{{ for id,lib in groupes: }}<option value="{{ =id }}">{{ =lib }}</option>{{ pass }}</select>
        <select id="region-filter"><option value="ALL">Toutes les régions</option>{{ for r in regions: }}<option value="{{ =r }}">{{ =r }}</option>{{ pass }}</select>
        <select id="depute-sort">{{ for k,v in tris: }}<option value="{{=k}}">{{=v}}</option>{{ pass }}</select>
        <select id="depute-dir"><option value="1">Tri ascendant</option><option value="-1">Tri descendant</option></select>
        <div><input id="depute-searchtext" type="text" placeholder="" /><input id="depute-searchbutton" type="button" value="Rechercher" />
        </div>
        <div id="depute-liste">
         
        </div>
        

        
    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script src="https://unpkg.com/infinite-scroll@3/dist/infinite-scroll.pkgd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.0.10/handlebars.min.js"></script>
    <script src="{{=URL('static','js/page_deputes.js')}}"></script>
    </body>
    
</html>
